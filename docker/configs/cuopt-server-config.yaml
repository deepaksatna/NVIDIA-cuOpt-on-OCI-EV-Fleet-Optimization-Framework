server:
  host: 0.0.0.0
  port: 8000
  workers: 4

logging:
  level: INFO
  format: json

optimization:
  max_time_limit: 300
  default_time_limit: 30
  enable_cache: true
  cache_size_mb: 1024

metrics:
  enabled: true
  port: 8001
  path: /metrics

health:
  enabled: true
  path: /health
